# 8/25 라이브 후속 자동화 점검 (2026-08-26)

## 결정 박스

| 항목 | 결과 |
|---|---|
| 라운지 등록 | **정상** — 8/26 11:34 KST 완료 |
| 카페 자동 발행 | **안 됨. 그리고 한 번도 자동으로 된 적 없음** |
| 원인 성격 | 코드 버그가 아니라 **운영 전환 미완료 + 최종 수정분 미푸시** |
| 맥 쪽 조치 | **완료** — 미커밋 수정분 커밋·푸시함 (아래 6장) |
| 남은 것 | **윈도우 운영기 4단계** (아래 5장). 실행 주체는 윈도우 그대로 유지 |

실행 주체를 맥으로 옮기지 않는 이유: 운영기는 상시 켜져 있고, Edge Studio 로그인 세션과
DPAPI로 묶인 OAuth 토큰이 그 계정에만 있다. 맥으로 옮기면 다운로드 인증부터 다시 깨진다.

---

## 1. 라운지 — 정상

| 확인 항목 | 값 |
|---|---|
| 코스 | `라이브 다시보기 - AI` (id 28) |
| 레슨 | id 139 · sortOrder 18 |
| 제목 | `2026-08-25 왕초보도 AI 영상 제작 딸깍으로 된다고?` |
| 영상 | https://youtu.be/b0sw90R32Q8 |
| 공개범위 | `unlisted` (일부공개) — 규격대로 |
| 업로드 시각 | 2026-08-26 11:34 KST |

주의 한 가지: 예약은 수 03:10(primary) → 05:10(retry) → 10:10(final) 3회인데,
업로드가 11:34에 끝났습니다. **10:10 final 슬롯에서야 성공했다는 뜻**이고,
앞의 두 번은 실패했을 가능성이 큽니다. 확정하려면 윈도우 `logs\scheduled-*.log`를 봐야 합니다.
(8/18 건도 같은 패턴이었습니다.)

수업자료(PDF·노션)는 비어 있습니다. 다만 이건 이 자동화의 범위 밖입니다.

## 2. 카페 — 안 올라갔고, 자동으로 올라간 적이 없음

게시판 `26321967 / 315` 최신글 실측:

| 글번호 | 제목 | 등록 시각 |
|---|---|---|
| 5959 | AI 로 영상편집 자동화 이렇게 쉽다고? | **2026-08-21 00:37 KST** |
| 5854 | 전자책 AI 자동화 이렇게 쉽다고? | 2026-08-12 12:27 |

- 8/25 라이브 글은 **없습니다.**
- 5959는 8/18 라이브 글인데, 등록 시각이 예약 실행 시간대(수 03:10/05:10/10:10)가 아니라
  **개발·검증을 하던 8/21 새벽 00:37**입니다. 즉 사람이 발행한 글입니다.
- 따라서 "라이브 끝나고 자동으로 카페에 올라간" 사례는 아직 0건입니다.

## 3. 원인 (측정된 근거)

### 3-1. 카페 단계는 기본이 꺼짐이고, 켜진 흔적이 없음

`scripts/run-scheduled-pipeline.mjs`는 라운지 등록까지 끝난 뒤 이렇게 갈립니다.

```js
const cafe = normalizeCafePublisherConfig(config, rootDir);
if (!cafe.enabled) {
  console.log("Cafe publisher is disabled; replay automation finished without a Cafe write.");
  return;
}
```

그런데 Git에 추적되는 `make-youtube-automation/config.windows.json`에는
**`cafePublisher` 블록 자체가 없습니다.** 없으면 `enabled=false`로 계산되어 위에서 그대로 종료됩니다.
블록이 있는 건 `config.example.json`뿐이고 그마저 `enabled:false, mode:"dry", notify:false`입니다.

### 3-2. 8/20 설정 분리 이후 예약 작업 재등록이 필요한 상태

커밋 `9359bea`(8/20 15:51)에서 운영 설정을 Git 밖 `config.local.json`으로 빼도록 바꿨습니다.
그런데 예약 작업이 그 파일을 쓰려면 아래로 **재등록**해야 합니다.

```powershell
.\scripts\install-windows.ps1 -Config config.local.json
```

재등록하지 않았다면 기존 작업은 `-Config` 없이 실행되고,
`invoke-scheduled.ps1`의 기본값인 `config.windows.json`(= 카페 블록 없는 추적 파일)을 읽습니다.

### 3-3. 운영 전환 7단계가 완료된 흔적 없음

navercafe README의 운영 전환 순서 7단계는
"게시글 읽기 검증과 Telegram 전송까지 한 번 통과한 뒤 예약 설정을 `mode=publish`, `notify=true`로 바꾼다"입니다.
MF 텔레그램방에 **카페 발행 알림이 한 번도 온 적이 없습니다.** 이 게이트를 통과한 적이 없다는 뜻입니다.

### 3-4. 결정적 — 마지막 수정분이 맥에만 남아 있음

두 저장소 모두 8/20 밤~8/21 새벽 작업분이 커밋되지 않은 채 맥 작업트리에 있습니다.
윈도우 PC가 pull해도 이 버전을 받을 수 없습니다.

`~/Projects/youtube` (브랜치 `codex/live-replay-session-refresh`, origin 대비 **ahead 1** + 미커밋)

| 파일 | 내용 |
|---|---|
| `lib/cafe-pipeline.mjs` | 카페 제목에서 앞 `YYYY-MM-DD` 제거하는 `cafeArticleTitle()` 추가 |
| `lib/live-schedule.mjs` | `sourceUrl`을 `youtu.be/ID` → `youtube.com/watch?v=ID`(회원 시청용)로 변경 |
| `docs/live-replay-automation-runbook.md`, `README.md`, 테스트 2종 | 위 두 변경에 맞춘 문서·테스트 |

`~/Projects/navercafe` (브랜치 `feat/youtube-cafe-publisher-only`, 미커밋)

| 파일 | 변경량 |
|---|---|
| `notebook_cafe_auto.py` | +12 |
| `youtube_cafe_auto.py` | +33 |
| `tests/test_publisher_contract.py` | +63 |
| `README.md` | OG 카드 → 회원용 직접 링크로 규격 변경 |

즉 **기준글 5854 포맷 검증을 통과시킨 바로 그 코드가 운영기에 없습니다.**

## 4. 확인 못 한 것

윈도우 운영 PC가 Tailscale에 없어(맥·s24-ultra만 온라인) 맥에서 원격으로 볼 수 없습니다.
아래 3가지는 윈도우에서 직접 봐야 확정됩니다.

1. `make-youtube-automation\logs\scheduled-primary-20260826-*.log` — 03:10·05:10이 왜 실패했는지
2. `config.local.json`의 `cafePublisher.enabled` / `mode` 실제 값
3. `Get-ScheduledTask AIMAX-Live-Replay-*`의 Arguments에 `-Config config.local.json`이 있는지

## 5. 윈도우 운영기에서 할 것 (남은 작업)

1. `git pull` 후 CafePublisher 설치본(`C:\Users\likim\AppData\Local\AIMAX\CafePublisher\`) 갱신
2. `config.local.json`에 아래 블록을 넣고 `enabled=true`, `mode="draft"`, `notify=false`로 시작

   ```json
   "cafePublisher": {
     "enabled": true,
     "mode": "draft",
     "python": "C:\\Users\\likim\\AppData\\Local\\Programs\\Python\\Python312\\python.exe",
     "script": "C:\\Users\\likim\\AppData\\Local\\AIMAX\\CafePublisher\\notebook_cafe_auto.py",
     "template": "reference-5854",
     "imageCount": 5,
     "notify": false,
     "expectedClubId": "26321967",
     "expectedMenuId": "315"
   }
   ```

3. **예약 작업 3개 재등록** — 이걸 빼면 계속 옛 `config.windows.json`을 읽어 카페가 꺼진 채로 돈다

   ```powershell
   .\scripts\install-windows.ps1 -Config config.local.json
   ```

4. 8/25 건으로 수동 1회 실행 → 임시저장본에서 날짜 없는 제목·본문 6구간·이미지 5장·회원용 href 1개 확인

   ```powershell
   node scripts\run-scheduled-pipeline.mjs --config config.local.json `
     --date 2026-08-25 --kind ai
   ```

5. 통과하면 `mode="publish"`, `notify=true`로 전환. 다음 수요일부터 자동으로 올라간다.

곁들여 볼 것: `logs\scheduled-primary-20260826-*.log`, `scheduled-retry-*` — 03:10·05:10이 왜 실패했는지.
매주 10:10 마지막 슬롯에서만 성공하는 상태는 여유가 없다.

## 6. 맥에서 한 조치 (완료)

| 저장소 | 커밋 | 내용 |
|---|---|---|
| `~/Projects/youtube` | `81bc3c6` | 카페 제목 날짜 접두사 제거, `sourceUrl`을 회원용 `watch?v=` 형식으로 |
| `~/Projects/youtube` | `f13978b` | 이 점검 보고서 |
| `~/Projects/navercafe` | `accd61f` | 원본 링크를 OG 카드 대신 직접 링크로, 발행 검증을 정확한 href 대조로 |

- 검증: youtube `npm test` 17건 통과, navercafe 테스트 25건 통과(맥에 없는 윈도우 전용 의존성은 스텁 처리)
- 시크릿 스캔 후 두 저장소 모두 push 완료, 원격 커밋 대조 확인
- `server/plaud/extension-upload.js`, `server/server.js`의 미커밋 변경은 **PLAUD 업로드 서버 건이라 손대지 않았습니다.** 별도로 판단이 필요합니다.
